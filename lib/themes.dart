import 'package:flutter/material.dart';

// Light mode theme
class LightTheme {
  static const primaryColorLight = Colors.purple;
  static const secondaryColorLight = Colors.grey;
  static const backgroundColorLight = Colors.white;
}

//Dark mode theme
class DarkTheme {
  static const primaryColorDark = Color(0xFF2697FF);
  static const secondaryColorDark = Color.fromARGB(237, 30, 40, 62);
  static const backgroundColorDark = Color.fromARGB(255, 28, 29, 39);
  static const textColorDark = Color.fromARGB(255, 160, 161, 162);
}

